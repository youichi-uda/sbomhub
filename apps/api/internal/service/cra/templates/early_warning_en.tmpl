# CRA Article 14 Early Warning (within 24 hours)

> **Legal notice**
> This document is a draft generated by SBOMHub. Final review and approval are the responsibility of the manufacturer.
> SBOMHub provides submission support through drafting; it does not provide legal advice.
> The start of the 24-hour clock and the decision to submit to the European CSIRT / ENISA must be made by the manufacturer's authorised representative.

## 1. Report classification

- **Report type**: CRA Article 14(2)(a) Early Warning
- **Deadline**: within 24 hours of awareness
- **Language**: English

## 2. Product information

- **Product name**: {{ .ProductName }}
{{- if .ProductVersion }}
- **Product version**: {{ .ProductVersion }}
{{- end }}
{{- if .VendorName }}
- **Manufacturer**: {{ .VendorName }}
{{- end }}

## 3. Vulnerability summary

- **CVE ID**: {{ .CVEID }}
{{- if .CVSSScore }}
- **CVSS score**: {{ .CVSSScore }}{{ if .CVSSVector }} ({{ .CVSSVector }}){{ end }}
{{- end }}
{{- if .Severity }}
- **Severity**: {{ .Severity }}
{{- end }}
- **Description**:

{{ if .VulnerabilitySummary }}{{ .VulnerabilitySummary }}{{ else }}_(to be filled in)_{{ end }}

## 4. Exploitation status

- **Exploitation determination**: {{ .ExploitationStatus }}
{{- if .KEVListed }}
- **CISA KEV listed**: yes
{{- end }}
{{- if .EPSSScore }}
- **EPSS score**: {{ .EPSSScore }}
{{- end }}
{{- if .ExploitationEvidence }}
- **Evidence of exploitation**:

{{ .ExploitationEvidence }}
{{- end }}

## 5. Preliminary impact scope

{{ if .PreliminaryImpactScope }}{{ .PreliminaryImpactScope }}{{ else }}_(record the preliminary impact scope. Final information will be updated in the 72-hour detailed notification.)_{{ end }}

{{ if .AffectedComponents }}
### Preliminary affected components

{{ range .AffectedComponents }}
- {{ .Name }}{{ if .Version }} (version: {{ .Version }}){{ end }}{{ if .PURL }} `{{ .PURL }}`{{ end }}
{{- end }}
{{ end }}

## 6. Immediate mitigations applied

{{ if .ImmediateMitigations }}{{ .ImmediateMitigations }}{{ else }}_(temporary mitigations already in place. More detail will be provided in the 72-hour notification.)_{{ end }}

## 7. Reporter

- **Name**: {{ if .ReporterName }}{{ .ReporterName }}{{ else }}_(to be filled in)_{{ end }}
- **Role**: {{ if .ReporterRole }}{{ .ReporterRole }}{{ else }}_(to be filled in)_{{ end }}
- **Contact email**: {{ if .ContactEmail }}{{ .ContactEmail }}{{ else }}_(to be filled in)_{{ end }}
{{- if .ContactPhone }}
- **Contact phone**: {{ .ContactPhone }}
{{- end }}

## 8. Submission metadata

- **Submitted at (UTC)**: {{ .SubmittedAt }}
{{- if .AwarenessTime }}
- **Awareness time (UTC, start of 24h clock)**: {{ .AwarenessTime }}
{{- end }}
{{- if .ReportID }}
- **Report ID (internal)**: {{ .ReportID }}
{{- end }}

---

_This document is a draft generated by SBOMHub ({{ .GeneratedBy }}) at {{ .GeneratedAt }}._
_Manufacturer review and approval are required before submission._
