{
  "name": "@sbomhub/mcp-server",
  "version": "0.1.0",
  "description": "MCP Server for SBOMHub",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "sbomhub-mcp": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js",
    "//test": "The tests drive dist/index.js, so they build first. test/run.mjs enumerates test/**/*.test.mjs itself and hands the list to `node --test` — no glob (Windows) and no directory argument (removed in Node 24), and no hand-maintained file list that could omit a file.",
    "test": "tsc && node test/run.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.19.7",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
