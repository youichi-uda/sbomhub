# CRA 第14条 早期警告 (24時間以内通知)

> **法的注記**
> 本文書は SBOMHub による下書きであり、最終的な確認・承認は事業者が行うものとします。
> SBOMHub は提出支援のためのドラフト生成を行いますが、法的助言を提供するものではありません。
> 24時間カウントの起点判断、および欧州 CSIRT / ENISA への提出可否は、事業者の責任者が決定してください。

## 1. 報告区分

- **報告種別**: CRA 第14条(2)(a) 早期警告 (Early Warning)
- **対象期限**: 認識後 24 時間以内
- **言語**: 日本語

## 2. 製品情報

- **製品名**: {{ .ProductName }}
{{- if .ProductVersion }}
- **製品バージョン**: {{ .ProductVersion }}
{{- end }}
{{- if .VendorName }}
- **製造業者**: {{ .VendorName }}
{{- end }}

## 3. 脆弱性概要

- **CVE ID**: {{ .CVEID }}
{{- if .CVSSScore }}
- **CVSS スコア**: {{ .CVSSScore }}{{ if .CVSSVector }} ({{ .CVSSVector }}){{ end }}
{{- end }}
{{- if .Severity }}
- **重大度**: {{ .Severity }}
{{- end }}
- **脆弱性の概要**:

{{ if .VulnerabilitySummary }}{{ .VulnerabilitySummary }}{{ else }}_（要記入）_{{ end }}

## 4. 悪用状況 (Exploitation Status)

- **悪用状況の判定**: {{ .ExploitationStatus }}
{{- if .KEVListed }}
- **CISA KEV 収載**: はい
{{- end }}
{{- if .EPSSScore }}
- **EPSS スコア**: {{ .EPSSScore }}
{{- end }}
{{- if .ExploitationEvidence }}
- **悪用の根拠**:

{{ .ExploitationEvidence }}
{{- end }}

## 5. 暫定影響範囲 (Preliminary Impact Scope)

{{ if .PreliminaryImpactScope }}{{ .PreliminaryImpactScope }}{{ else }}_（暫定的に確認された影響範囲を記入。確定情報は 72 時間後の詳細通知で更新する。）_{{ end }}

{{ if .AffectedComponents }}
### 暫定影響コンポーネント

{{ range .AffectedComponents }}
- {{ .Name }}{{ if .Version }} (バージョン: {{ .Version }}){{ end }}{{ if .PURL }} `{{ .PURL }}`{{ end }}
{{- end }}
{{ end }}

## 6. 現時点で講じた緩和措置 (Immediate Mitigations)

{{ if .ImmediateMitigations }}{{ .ImmediateMitigations }}{{ else }}_（既に実施した暫定的な緩和策。72 時間通知でより詳細な情報を提供する。）_{{ end }}

## 7. 報告者情報

- **報告者氏名**: {{ if .ReporterName }}{{ .ReporterName }}{{ else }}_（要記入）_{{ end }}
- **役職**: {{ if .ReporterRole }}{{ .ReporterRole }}{{ else }}_（要記入）_{{ end }}
- **連絡先メール**: {{ if .ContactEmail }}{{ .ContactEmail }}{{ else }}_（要記入）_{{ end }}
{{- if .ContactPhone }}
- **連絡先電話**: {{ .ContactPhone }}
{{- end }}

## 8. 提出メタ情報

- **提出日時 (UTC)**: {{ .SubmittedAt }}
{{- if .AwarenessTime }}
- **認識日時 (UTC, 24h カウント起点)**: {{ .AwarenessTime }}
{{- end }}
{{- if .ReportID }}
- **報告 ID (社内管理)**: {{ .ReportID }}
{{- end }}

---

_本書は SBOMHub ({{ .GeneratedBy }}) により {{ .GeneratedAt }} に生成された下書きです。_
_提出前に事業者の最終確認・承認を経てください。_
